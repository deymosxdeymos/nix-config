#!/bin/bash
echo "Error: git is disabled. Use jj instead:" >&2
echo "" >&2
echo "  git add FILE      ->  jj file track FILE" >&2
echo "  git status        ->  jj st" >&2
echo "  git diff          ->  jj diff" >&2
echo "  git log           ->  jj log" >&2
echo "  git commit        ->  jj describe / jj commit" >&2
echo "  git push          ->  jj git push" >&2
echo "  git pull / fetch  ->  jj git fetch" >&2
echo "" >&2
echo "jj subcommands (jj git push, jj git fetch) are fine and are not blocked." >&2
exit 1
